@model IEnumerable<Category>

<div class="col-6">
    <a asp-controller= "Category" asp-action = "Create" class="btn btn-primary">
    Add new Category
    </a>
</div>

@if (Model.Count() > 0)
{
    @foreach (var obj in Model)
    {
        <p>@obj.Id  @obj.Name  @obj.DisplayOrder</p>
    }
    
}
else
{
    <p>No categories exists.</p>
}